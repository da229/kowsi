public class Solution {
    public int solve(int A, int B) {
        // For all four sides, we need to pick the minimum of horizontal 
        // and vertical distnace to the end of the board as the 
        // possible number of squares.
        // Doing this on all four sides...
        
        return Math.min(A-1, B-1) + Math.min(8-A, B-1) 
                + Math.min(A-1, 8-B) + Math.min(8-A, 8-B);
    }
}

